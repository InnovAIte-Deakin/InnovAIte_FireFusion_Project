import MisinfoLanding from "../components/misinfo/MisinfoLanding.jsx";

/** Screen 1 — Misinformation monitor (Figma). Route: `/alerts` */
export default function Alerts() {
  return <MisinfoLanding />;
}
