pytest>=8.0
pytest-asyncio>=0.23
httpx>=0.27

# The unit tests import the real ForecastService, so its runtime dependencies
# must be present or every unit test silently skips.
redis>=5.0
fastapi>=0.115
pydantic-settings>=2.0
aio_pika>=9.0