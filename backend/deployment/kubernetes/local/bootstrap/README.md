# Legacy local bootstrap path

This directory is kept only for compatibility with some Windows/OneDrive workspaces that had the earlier bootstrap path checked out.

Use `backend/deployment/kubernetes/local-bootstrap` for local dependency-first bootstrap rendering and deployment.
