apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Deprecated compatibility stub.
# Use ../../local-bootstrap instead. Keeping this file avoids directory-deletion
# issues on some Windows/OneDrive workspaces while removing the previous cycle.
resources: []
