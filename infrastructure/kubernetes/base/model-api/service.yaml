apiVersion: v1
kind: Service
metadata:
  name: model-api
  namespace: firefusion
  labels:
    app.kubernetes.io/name: model-api
    app.kubernetes.io/part-of: firefusion-platform

spec:
  type: ClusterIP

  selector:
    app.kubernetes.io/name: model-api

  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: http
